var ledCtrl = angular.module('ledCtrl', []);
app.controller('ledCtrl', [ '$scope', '$location','$http','$rootScope',
    function($scope, $location, $http, $rootScope ) {
			$scope.sistema = 'CapiteWeb';

			
}]
);

	
